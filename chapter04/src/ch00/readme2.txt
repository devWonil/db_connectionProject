 <Client>

 변수 선언

﻿
 Vector는 ArrayList와 동일한 내부 구조를 가지며 값이 추가되면 자동으로 크기가 조절된다.
 Vector는 동기화된 메소드로 구성되어 있기 때문에 멀티 쓰레드가 동시에 이 메소드들을 실행할 수 없고
 하나의 쓰레드가 실행을 완료해야만 다른 쓰레드들이 실행할 수 있다.
 멀티 쓰레드 환경에서 안전하게 객체를 추가하고 삭제 할 수 있다.

﻿ 문자열 분리 StringTokenizer
 해당 프로토콜을 이용해서 문자열을 분리하여 쪽지 보낼 때 쓰는 기능
 

 connectServer
 소켓에 연결이 안되었을시 상황에 맞는 예외를 처리함
 
 netWork
 벡터의 유저의 id를 저장하고 리스트화면에 추가
 
 inmessage
 
 sendmessage
 메시지 보내기
 
 <Server>
 유저 정보와 방 정보를 내부 클래스 타입으로 벡터에 담음
 
 
 